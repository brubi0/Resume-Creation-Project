# Output Folder

Quick-start landing zone for generated files. When a candidate session is started, outputs are saved to `candidates/[name]/output/` instead.

This folder is used when running the workflow without creating a candidate directory first.
