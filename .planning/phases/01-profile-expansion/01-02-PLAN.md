---
phase: 01-profile-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - system/workflow.md
  - profiles/README.md
autonomous: true
requirements:
  - PROF-04

must_haves:
  truths:
    - "User can clone an existing profile for a related role without starting from scratch"
    - "Workflow.md Phase 0 step 3 explicitly offers the clone-and-tweak path when a near-match profile exists"
    - "profiles/README.md documents the clone procedure so users know it is available"
    - "A cloned profile is clearly named for the new role, not a copy of the source filename"
  artifacts:
    - path: "system/workflow.md"
      provides: "Phase 0 step 3 updated to offer clone-and-tweak when a near-match profile exists, with specific instructions on which fields to change"
      contains: "clone"
    - path: "profiles/README.md"
      provides: "Clone procedure documented: which fields to change, naming convention, when to clone vs. generate fresh"
      contains: "clone"
  key_links:
    - from: "system/workflow.md"
      to: "profiles/README.md"
      via: "clone procedure cross-reference (optional but preferred)"
      pattern: "README"
    - from: "profiles/README.md"
      to: "profiles/netsuite_administrator.md"
      via: "listed as clonable source profile in the Existing Profiles table"
      pattern: "netsuite_administrator"
---

<objective>
Add the clone-and-tweak path to the profile system so users can fork an existing profile for a related role instead of generating from scratch. Right now workflow.md Phase 0 step 3 only asks whether the profile "fits" — there is no guidance on partially-matching profiles or on how to clone one.

Purpose: PROF-04 — existing profiles can be cloned and tweaked for related roles. A Financial Controller shouldn't need a full web search when the NetSuite Administrator profile already has 80% of the relevant categories.
Output: Updated workflow.md Phase 0 step 3 + updated profiles/README.md with clone procedure.
</objective>

<execution_context>
@C:/Users/BXR/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BXR/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-profile-expansion/01-CONTEXT.md
@.planning/phases/01-profile-expansion/01-01-SUMMARY.md

<interfaces>
<!-- Current workflow.md Phase 0 step 3 (pre-edit state after Plan 01 ran): -->
```
3. If a matching profile exists, confirm it fits or offer to tweak it
```

<!-- Current profiles/README.md (full file — 26 lines): -->
```
# Profiles

Industry/role profiles that define the skills matrix categories and proficiency levels for a target role.

## Existing Profiles

| Profile | Target Roles |
|---------|-------------|
| `netsuite_administrator.md` | NetSuite Admin, NetSuite Consultant, ERP Analyst, Controller |

## Creating a New Profile

Profiles are auto-generated during Phase 0 of the resume workflow. Claude will:

1. Ask about the candidate's industry and target role
2. Research current job postings to identify required skills
3. Group skills into categories with proficiency definitions
4. Save the profile here for reuse

## Profile Format

Each profile contains:
- Industry and target roles
- Proficiency level definitions (with colors for the skills matrix)
- Skills organized by category with descriptions of what each skill covers
```

<!-- netsuite_administrator.md Target Roles line (shows related-role pattern): -->
**Target Roles:** NetSuite Administrator, NetSuite Consultant, ERP Analyst, Financial Controller, Accounting Manager
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update system/workflow.md Phase 0 step 3 with clone-and-tweak path</name>
  <files>system/workflow.md</files>
  <read_first>
    - system/workflow.md — read fully before editing; only Phase 0 step 3 changes, all other content stays intact
    - .planning/phases/01-profile-expansion/01-CONTEXT.md — note the "simple approach preferred" discretion for clone mechanism
  </read_first>
  <action>
In `system/workflow.md`, find Phase 0 step 3 (currently reads: "If a matching profile exists, confirm it fits or offer to tweak it"). Replace that single-line step 3 with the following block:

```
3. Match the profile to the candidate's role using this logic:
   - **Exact or close match** (profile covers the target role or a near-equivalent): Confirm it fits and proceed
   - **Partial match** (profile covers a related role — e.g., candidate wants Financial Controller and you have NetSuite Administrator): Offer to clone and tweak:
     - Copy the source profile to `profiles/[new_role_slug].md` (e.g., `financial_controller.md`)
     - Update the header: `# Profile: [New Role Name]`, `**Industry:**`, `**Target Roles:**`
     - Review each skill category: remove irrelevant ones, rename any that need to shift focus, add 1-3 new categories for skills unique to the new role
     - Save and confirm with user before proceeding
   - **No match**: Generate a new profile — see step 4
```

Do not change any other content in workflow.md — Phase 0 steps 1, 2, 4, and 5, and all other phases, remain exactly as-is.
  </action>
  <verify>
    <automated>grep -n "clone\|Partial match\|new_role_slug" "C:/Users/BXR/OneDrive/Documents/GitHub/Resume Creation Project/system/workflow.md"</automated>
  </verify>
  <acceptance_criteria>
    - `system/workflow.md` contains the string "clone" in Phase 0 — verifiable with grep
    - `system/workflow.md` contains "Partial match" or "partial match" in Phase 0 — verifiable with grep
    - `system/workflow.md` contains "new_role_slug" (naming convention for cloned files) — verifiable with grep
    - `system/workflow.md` contains "Exact or close match" in Phase 0 — verifiable with grep
    - Phase 0 step 4 still contains "profile-generation-guide.md" (unchanged from Plan 01) — verifiable with grep
    - All phases other than Phase 0 step 3 are unchanged
  </acceptance_criteria>
  <done>Workflow.md Phase 0 step 3 now has three explicit paths: exact match (proceed), partial match (clone and tweak), no match (generate new). A Claude instance reading this will know exactly when and how to clone.</done>
</task>

<task type="auto">
  <name>Task 2: Update profiles/README.md with clone procedure and usage guidance</name>
  <files>profiles/README.md</files>
  <read_first>
    - profiles/README.md — read the full file before editing; it is short (26 lines) and must be updated, not replaced wholesale
    - profiles/netsuite_administrator.md — used as the example clonable source in the documentation
  </read_first>
  <action>
Rewrite `profiles/README.md` with the following content (this replaces the entire file — the existing content is too thin to patch incrementally):

```markdown
# Profiles

Industry/role profiles that define the skills matrix categories and proficiency levels for a target role. Each profile is consumed by Phase 0 (profile selection) and Phase 2 (skills matrix discovery).

## Existing Profiles

| Profile | Target Roles |
|---------|-------------|
| `netsuite_administrator.md` | NetSuite Admin, NetSuite Consultant, ERP Analyst, Financial Controller, Accounting Manager |

## Creating a New Profile

Profiles are auto-generated during Phase 0 of the resume workflow when no matching profile exists. Claude will:

1. Search for 3-5 current job postings for the target role (or use user-provided postings)
2. Extract common skills and group them into 8-20 categories
3. Define proficiency levels using the standard 4-level system
4. Save the profile to `profiles/[role_slug].md`

See `system/profile-generation-guide.md` for the full generation protocol.

## Cloning an Existing Profile

When a candidate's target role is closely related to an existing profile, clone and tweak instead of generating from scratch:

1. Copy the source profile: `cp profiles/netsuite_administrator.md profiles/[new_role_slug].md`
2. Update the header (`# Profile:`, `**Industry:**`, `**Target Roles:**`)
3. Review each skill category:
   - Remove categories irrelevant to the new role
   - Rename categories whose focus needs to shift
   - Add 1-3 new categories for skills unique to the new role
4. Save and confirm with user

**When to clone vs. generate fresh:**
- Clone: source profile covers 60%+ of the target role's skill areas (e.g., Financial Controller from NetSuite Administrator)
- Generate fresh: target role is in a different domain or the overlap is less than 60%

## Profile Format

Each profile contains:
- Header: industry and target roles (plus a `**Source:** Knowledge-based` flag if not research-backed)
- Proficiency level table (4 levels: Expert, Advanced, Intermediate, Novice with hex color codes)
- Skills organized into category groups, each with a descriptive paragraph

Profile filenames use snake_case: `financial_controller.md`, `frontend_engineer.md`, `data_analyst.md`.
```
  </action>
  <verify>
    <automated>grep -n "clone\|Clone\|profile-generation-guide\|snake_case\|60%" "C:/Users/BXR/OneDrive/Documents/GitHub/Resume Creation Project/profiles/README.md"</automated>
  </verify>
  <acceptance_criteria>
    - `profiles/README.md` contains the string "clone" or "Clone" — verifiable with grep
    - `profiles/README.md` contains "profile-generation-guide.md" (cross-reference to generation guide from Plan 01) — verifiable with grep
    - `profiles/README.md` contains "snake_case" (naming convention) — verifiable with grep
    - `profiles/README.md` contains "60%" (clone vs. generate decision rule) — verifiable with grep
    - `profiles/README.md` still lists `netsuite_administrator.md` in the Existing Profiles table — verifiable with grep
    - File is at least 50 lines long (substantive update, not a stub) — verifiable with line count
  </acceptance_criteria>
  <done>profiles/README.md now documents both creation paths (generate and clone), gives a concrete decision rule (60% overlap threshold), cross-references the generation guide, and documents the naming convention.</done>
</task>

</tasks>

<verification>
After both tasks complete, run:

```bash
grep -n "clone\|Partial match" "C:/Users/BXR/OneDrive/Documents/GitHub/Resume Creation Project/system/workflow.md"
grep -n "profile-generation-guide\|60%\|snake_case" "C:/Users/BXR/OneDrive/Documents/GitHub/Resume Creation Project/profiles/README.md"
grep -c "" "C:/Users/BXR/OneDrive/Documents/GitHub/Resume Creation Project/profiles/README.md"
```

Expected: workflow.md has clone and Partial match in Phase 0; README.md references generation guide, has 60% rule, has snake_case, is at least 50 lines.
</verification>

<success_criteria>
- workflow.md Phase 0 step 3 has three clear paths: exact match, partial match (clone), no match (generate)
- profiles/README.md documents both creation flows (generate and clone) with a concrete decision rule
- Any user reading README.md understands when and how to clone a profile for a related role
- PROF-04 is implementable end-to-end with no ambiguity
</success_criteria>

<output>
After completion, create `.planning/phases/01-profile-expansion/01-02-SUMMARY.md`
</output>
